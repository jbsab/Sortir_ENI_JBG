{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="d-flex justify-content-center">
    <button class="btn btn-success m-2">{{ button_label|default('Enregistrer') }}</button>
    <button class="btn btn-primary m-2" name="publier" value="Publier">{{ button_label|default('Publier') }}</button>
    <button class="btn btn-warning m-2" name="annuler" value="Annuler">{{ button_label|default('Annuler la sortie') }}</button>
</div>

{{ form_end(form) }}
