{% extends 'base.html.twig' %}

{% block title %}Edit Participant{% endblock %}

{% block body %}
    <div class="container-fluid container p-2 card shadow">
        <div class="d-flex justify-content-center">
            <h1>Edit Participant</h1>
        </div>


            {{ include('participant/_form.html.twig',{'button_label': 'Update'}) }}

        <div class="d-flex justify-content-center">
            <a href="{{ path('app_participant_index') }}" class=" btn btn-primary btn-sm text-decoration-none mt-3">back to list</a>
        </div>
            <div class="container-fluid container-md mt-3">
    {{ include('participant/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
