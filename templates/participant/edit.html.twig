{% extends 'base.html.twig' %}

{% block title %}Modifier le profil{% endblock %}

{% block body %}
    {% if is_granted("ROLE_USER")%}
    {% if app.user == participant %}
    <div class="container-sm p-2 card shadow">
        <div class="d-flex justify-content-center">
            <h1>Modifier le profil</h1>
        </div>


            {{ include('participant/_form.html.twig',{'button_label': 'Mettre à jour'}) }}

        <div class="d-flex justify-content-center">
            <a href="{{ path('app_participant_index') }}" class=" btn btn-primary btn-sm text-decoration-none mt-3">Liste des participants</a>
        </div>
            <div class="mt-3">
    {{ include('participant/_delete_form.html.twig') }}
            </div>
    </div>
    {% endif %}
    {% endif %}
{% endblock %}
