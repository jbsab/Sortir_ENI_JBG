<header class="container-md container-fluid p-1 shadow rounded">

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <img src="{{ asset('media/favicon.ico') }}" width="50" height="50" class="custom-margin">

    <a class="navbar-brand" href="{{ path('sortir_main') }}">Sortir.com</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse mt-2 d-flex justify-content-center" id="navbarNav">
        <ul class="navbar-nav">
{#            <li class="nav-item">
                <a class="nav-link" href="{{path('app_ville_index')}}">Villes</a>
            </li>#}
            <li class="nav-item">
                <a class="nav-link" href="{{path('app_campus_index')}}">Campus</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('app_participant_index')}}">Participants</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('sortir_main') }}">Accueil</a>
            </li>
            {% if app.user %}
                <li class="mb-3 mt-2">
                    Connecté avec <a href="{{ path('app_participant_show', {'id': app.user.id}) }}">{{ app.user.userIdentifier }}</a>, <a href="{{ path('app_logout') }}" class="text-black text-decoration-none">Déconnexion</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sortir_register') }}">Créer un compte</a>
                </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a href="{{ path('admin') }}" class="nav-link">Admin</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>

</header>

<style>
    .custom-margin {
        margin-right: 8px;
    }
</style>

