{% extends 'base.html.twig' %}

{% block title %}Campus {{ campus.nom }}{% endblock %}

{% block body %}
<div class="container-md col-8 p-3 card shadow">
    <h1>Campus {{ campus.nom }}</h1>

    <table class="table table-bordered table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campus.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ campus.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_campus_index') }}">Retour à la liste des campus</a>

    <a href="{{ path('app_campus_edit', {'id': campus.id}) }}">Modifier le campus</a>

    {{ include('campus/_delete_form.html.twig') }}
</div>
{% endblock %}
